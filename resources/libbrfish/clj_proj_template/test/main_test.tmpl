(ns {{top/ns}}.{{main/ns}}-test
  (:require [clojure.test :refer [deftest is testing]]
            [{{top/ns}}.{{main/ns}} :as sut])) ; system under test

({{test-runner/deftest}} a-test
  ({{test-runner/testing}} "FIXME, I fail."
    ({{test-runner/is}} (= 0 1))))
